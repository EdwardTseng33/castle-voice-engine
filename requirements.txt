# castle-voice-engine - runtime deps (Python 3.11)
# (c) 2026 Edward / BeyondPath.
#
# v0.1.5 (2026-04-27): voice backend swapped to OpenAI Realtime API.
#   - PersonaPlex (NVIDIA NOML) deps removed: torch, transformers, opuslib.
#   - Moshi (Kyutai MIT) audio runtime removed.
#   - HuggingFace download path removed (no 14GB cold-start cost).
#   - Added: openai SDK + httpx (server-side ephemeral token mint).
# Legacy archive: castle/server/test_endpoints.py kept on disk but no longer
# wired into Modal app; its imports (torch / soundfile / moshi) are NOT
# installed at runtime and will fail if loaded -- intentional, archive only.

# --- Web framework + ASGI ---
fastapi>=0.110,<0.116
uvicorn[standard]>=0.29,<0.31

# --- WebSocket / wire (engine_server.py legacy /voice route) ---
websockets>=12.0,<13.0

# --- Persona registry ---
PyYAML>=6.0

# --- OpenAI Realtime ephemeral token mint ---
httpx>=0.27,<0.29
openai>=1.40,<2.0

# --- Modal deploy ---
modal>=0.65,<1.0

# --- v0.3.0 Phase 2 Path B (Anthropic Claude subagent dispatch + spaCy STT correction) ---
anthropic>=0.40,<0.80
spacy>=3.7,<3.9
