# secrets / credentials
.env
.env.*
!.env.example
.modal.toml
*.pem
*.key
*.crt
secrets.json
credentials.json

# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# build / runtime
*.log
*.tmp
dist/
build/
.DS_Store
Thumbs.db

# Phase 2 dispatch jsonl logs (跨 session 派工紀錄、含 Edward 講話內容、PRIVACY CRITICAL)
# 2026-05-22 蘇菲新增 · Phase 2 後半段 dispatch_handler runtime artifact
events/

# editor
.vscode/
.idea/
*.swp

# large local artifacts (PersonaPlex weights cache)
weights/
*.safetensors
*.bin
*.pth
*.pt
test_outputs/

# Phase 3.2 SoulX-FlashHead 1.3B Lite (6.4GB · gitignore 防漏)
models/
soulx_flashhead/
*.gguf
*.onnx

# 第三方 clone (Soul-AILab/SoulX-FlashHead · 不 commit 別人的 code)
external/
pretrained_models/
poc/*.wav
poc/*.mp4
poc/*.png

# Phase 3.2 蘇菲參考照片 binary (Edward 私密素材 · 不上 git)
assets/*.png
assets/*.jpg
assets/*.jpeg
# 但 README.md 上 (給下個對話蘇菲知道為什麼有這目錄 + 怎麼用)
!assets/README.md

# voice samples (PRIVACY CRITICAL - Edward 個人聲紋樣本不上 git)
# (Phase 1 PoC 2026-05-22 卡西法新增、跨 PoC 永遠不解禁)
voice_samples/
*.m4a
*.wav
*.mp3
*.opus
*.flac
*.aac
*.ogg

# Eagle 聲紋指紋檔 (含 Edward 聲紋特徵、PRIVACY CRITICAL)
*_speaker_profile.bin
edward_speaker_profile.bin
*.eagle
