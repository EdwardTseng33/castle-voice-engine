# v0.7 Gate 4 巡檢報告 · 馬魯克

> **歷史文件**（2026-05-23）· 已部分 supersede：本巡檢中所有「subject_guard」/「Sally hard rule」段落於 2026-05-26 v0.3.0 整批移除（記憶污染 · 詳 CHANGELOG）。下文相關段落僅供歷史記錄、不再 enforce。
>
> 日期：2026-05-23
> 巡檢對象：branch `voice-path/v0.7-musetalk-self-host` commit `b391902`
> 巡檢人：馬魯克（PM / QA Lead · Gate 4 版控守門人）
> 巡檢依據：spec_v0.7-self-host-musetalk-2026-05-23.md · v0.7-trust-audit-suliman.md · v0.7-plan-calcifer.md · v0.7-visual-spec-witch.md · lesson 2026-05-23 × 2

---

## § 1 · 六件 Check 結果

---

### Check 1 · 分支策略

**分支來源驗證：**

git graph 確認：

```
b590661 castle/main 最後一個 commit（merge base）
   ↓ 歷史延伸（所有 feature 在 voice-path/* 上）
...（Tavus v0.4.x / v0.5.0 walk）
dd550a6 (voice-path/v0.3.2-tavus-cvi-poc) docs: calcifer plan
b391902 (voice-path/v0.7-musetalk-self-host) ← 當前 HEAD
```

`voice-path/v0.7-musetalk-self-host` 是從 `voice-path/v0.3.2-tavus-cvi-poc` 頂端分出，merge-base 與 `castle/main` = `b590661`（v0.2.0）。分支策略符合計畫書 § 4 設計：main 不動、feature work 在獨立 branch 推進。

**main 污染確認：**

`castle/main` 最新 commit = `b590661 v0.2.0`，未收進任何 v0.3.x / v0.4.x / v0.7 工作，**主幹乾淨**。PASS。

**退版路徑（上一個能跑的版本）：**

v0.5.0 是「走錯架構但能跑」的最後線上版本，位於 commit `5fb61cf`，在 `voice-path/v0.3.2-tavus-cvi-poc` 上。

退版指令：

```bash
# 退回 v0.5.0（走錯架構但能跑）
cd "C:\Users\Administrator\Claude\castle-voice-engine"
git checkout voice-path/v0.3.2-tavus-cvi-poc
git reset --hard 5fb61cf   # v0.5.0 commit hash
# 重新 Modal deploy（對應 Tavus 架構）
modal deploy castle/server/app.py
```

注意：v0.5.0 用 Tavus end-to-end（非三層架構），退版等於退到「可跑但架構錯誤」狀態，僅作緊急保底用。

**v0.5.1 stash 確認：**

```
stash@{0}: On voice-path/v0.3.2-tavus-cvi-poc: v0.5.1-unship-pending-arch-fix
```

stash **存在且完整**。PASS。

**Verdict：PASS**

---

### Check 2 · 第 5 道 gate enforcement（憲法級 lesson 守住嗎）

**`app_musetalk.py` dependency pin 來源驗證：**

從 commit `b391902` 讀取 `breeze_poc/app_musetalk.py`，dependency 清單如下：

```python
"torch==2.0.1"
"torchvision==0.15.2"
"torchaudio==2.0.2"
extra_index_url="https://download.pytorch.org/whl/cu117"

"diffusers==0.30.2"
"accelerate==0.28.0"
"numpy==1.23.5"
"tensorflow==2.12.0"
"tensorboard==2.12.0"
"opencv-python==4.9.0.80"
"soundfile==0.12.1"
"transformers==4.39.2"
"huggingface_hub==0.30.2"
"einops==0.8.1"
```

卡西法 plan（v0.7-plan-calcifer.md § 2.1）表格列 MuseTalk 官方 tested matrix：

| Pin | 官方 tested（卡西法 § 2.1）| app_musetalk.py 實際 |
|---|---|---|
| torch | `torch==2.0.1+cu117` | `torch==2.0.1` + `extra_index_url=cu117` ✅ |
| diffusers | `diffusers==0.30.2` | `diffusers==0.30.2` ✅ |
| transformers | `transformers==4.39.2` | `transformers==4.39.2` ✅ |
| numpy | `numpy==1.23.5` | `numpy==1.23.5` ✅ |
| accelerate | `accelerate==0.28.0` | `accelerate==0.28.0` ✅ |
| tensorflow | `tensorflow==2.12.0` | `tensorflow==2.12.0` ✅ |
| opencv-python | `opencv-python==4.9.0.80` | `opencv-python==4.9.0.80` ✅ |

所有 critical pin **逐一對齊官方**，且 commit message 明文「dependency / matrix 全部來自 MuseTalk 官方 README + requirements.txt + inference.sh 比對」。**第 5 道 gate 落地**。

**`musetalk_client.py` 依賴既有 project integration 確認：**

```python
from castle.safety.subject_guard import (
    enforce_subject_whitelist,
    check_age_metadata,
    SubjectGuardError,
)
```

castle/safety/subject_guard 是既有 Phase 3.2 SoulX 整合時立的模組（commit `8d250be`），**複用正確**。外部新 dependency = 只有 `httpx`（thin HTTP client，合理）。PASS。

**既有 castle/server/ + breeze_poc/ 改動確認：**

`git diff voice-path/v0.3.2-tavus-cvi-poc voice-path/v0.7-musetalk-self-host -- castle/server/realtime_endpoints.py` = **無輸出（0 改動）**。

`castle/server/` 下新增的 `camera_endpoints.py` 是舊 v0.3.0 branch 帶過來的，不是本 v0.7 commit 新加（是 merge-base 之後從 v0.3.0 branch 繼承的已存在檔，非本 sprint 異動）。

`breeze_poc/app_breeze.py` 本 sprint 未動。PASS。

**Verdict：PASS**

---

### Check 3 · Sally hard rule 落地（沙利曼 P0 C12）

**`musetalk_client.py._check_subject()` 呼叫確認：**

```python
@staticmethod
def _check_subject(subject: str, age: Optional[int], operation: str) -> None:
    """Sally hard rule gate · raises SubjectGuardError on violation.

    sulima audit C12 requirement: every MuseTalk reference upload /
    inference must pass subject_guard before touching the model.
    """
    enforce_subject_whitelist(subject, operation)
    check_age_metadata(age, subject, operation)
```

兩個函式（`enforce_subject_whitelist` + `check_age_metadata`）**都在**，與沙利曼 C12 要求的 code path 完全對齊。

**`enroll_reference` 過 `_check_subject` 確認：**

```python
async def enroll_reference(self, image_bytes, subject, age=None):
    self._check_subject(subject, age, "musetalk.enroll_reference")  # ← 第一行
    ...
```

**PASS**。

**`stream_url` 過 `_check_subject` 確認：**

```python
def stream_url(self, subject: str, age: Optional[int] = None) -> str:
    self._check_subject(subject, age, "musetalk.stream_url")  # ← 第一行
    return f"{...}/musetalk/stream"
```

**PASS**。

**bypass / catch-and-ignore 確認：**

全讀 `musetalk_client.py`（133 行），無任何 `except SubjectGuardError: pass` 或同類 bypass 路徑。`_check_subject` 拋出 exception 直接傳播給 caller，不被吃掉。PASS。

**服務端補強確認（app_musetalk.py /musetalk/enroll）：**

服務端 `/musetalk/enroll` 有一段備注：

```python
# Subject guard runs on the client side (musetalk_client.py).
# Server-side defense-in-depth: re-check by re-importing rules.
# (Skipped here to avoid pulling castle.safety into Modal image.
#  Trust client + audit log on the server level.)
```

服務端目前「信任 client 端」沒有獨立跑 subject_guard，有 defense-in-depth 缺口。備注說明了原因（castle.safety 不進 Modal image）。

**評估：** client 端 100% 落地，服務端有書面說明的架構決策，是已知的 partial。沙利曼 audit C12 原文是「整合層必呼叫」，client 端已符合。這個服務端 gap 列為 WARN 供後續迭代補。

**Verdict：PASS（服務端 defense-in-depth 缺口 = WARN，列補強 backlog）**

---

### Check 4 · Attribution 落地（沙利曼 P0 C4）

**README.md MuseTalk attribution 段：**

```markdown
## Talking-head module (v0.7+)

This project uses **MuseTalk v1.5** (Lyra Lab, Tencent Music Entertainment)
under the **MIT License** for real-time lipsync.

Dependencies:
- OpenAI Whisper (MIT)
- IDEA-Research DWPose (Apache-2.0)
- ft-mse-vae (CreativeML Open RAIL-M · Track B audit required for commercial use)
- S3FD (license verification pending — see `docs/v0.7-trust-audit-suliman.md`)

Reference: https://github.com/TMElyralab/MuseTalk
```

**PASS** ✅（沙利曼 C4 attribution #1）

**`app_musetalk.py` file header attribution：**

```python
# MuseTalk v1.5 by Lyra Lab, Tencent Music Entertainment (MIT License)
# https://github.com/TMElyralab/MuseTalk
#
# Dependencies attribution:
#   - OpenAI Whisper (MIT)
#   - IDEA-Research DWPose (Apache-2.0)
#   - ft-mse-vae (CreativeML Open RAIL-M · Track B audit required for commercial use)
#   - S3FD (license verification pending)
```

**PASS** ✅（沙利曼 C4 attribution #2）

**`musetalk_client.py` file header attribution：**

```python
# MuseTalk v1.5 by Lyra Lab, Tencent Music Entertainment (MIT License)
# https://github.com/TMElyralab/MuseTalk
#
# Dependencies attribution:
#   - OpenAI Whisper (MIT)
#   - IDEA-Research DWPose (Apache-2.0)
#   - ft-mse-vae (CreativeML Open RAIL-M · Track B audit required for commercial use)
#   - S3FD (license verification pending)
```

**PASS** ✅（沙利曼 C4 attribution #2）

**log header（runtime stderr 第一行）：**

```python
logger.info(
    "[v0.7] talking-head: MuseTalk v1.5 (MIT, Lyra Lab/Tencent Music Entertainment)"
)
```

此行在 module load 時即執行（module-level），符合「runtime stderr 第一行」精神。**PASS** ✅（沙利曼 C4 attribution #4）

**對外 demo footer（Track B 項目）：**

v0.7 Track A 個人自用，沙利曼 audit § 1 明確「Track A 可省」對外 demo footer。**本 commit 不需要，免查**。

**四項 attribution 全落地（Track A 必要項全過）。**

**Verdict：PASS**

---

### Check 5 · 4 道 visual gate 守住嗎

**本 commit 是否涉及視覺改動：**

commit `b391902` 的 6 個異動檔案：

| 檔案 | 類型 | 視覺？ |
|---|---|---|
| README.md | 文件 | 否 |
| breeze_poc/app_musetalk.py | 後端 Python | 否 |
| breeze_poc/app_openai_tts.py | 後端 Python | 否 |
| castle/integrations/musetalk_client.py | 後端 Python | 否 |
| docs/v0.7-trust-audit-suliman.md | 文件 | 否 |
| docs/v0.7-visual-spec-witch.md | 文件（視覺 spec，非 CSS/HTML 實作） | 否 |

**結論：本 commit 0 個 castle/static/ 異動，4 道 visual gate 不適用**。

**Verdict：PASS（不適用・skip 正確）**

**下一批 visual gate 提醒（必讀・見 § 3）**

---

### Check 6 · `app_openai_tts.py` 順帶 commit 評估

**檔案來源：**

git log 顯示 `breeze_poc/app_openai_tts.py` 第一次出現在 commit `b391902`（`A` = Added），且此前所有 branch 都沒有這個檔案的 commit 記錄。但檔案內容顯示：

```python
# breeze_poc/app_openai_tts.py
# Voice Path v2.0 Phase 1 PoC - OpenAI gpt-4o-mini-tts
# 2026-05-22 calcifer · Edward compare OpenAI vs ElevenLabs · ship OpenAI first
```

日期 2026-05-22，屬於前一 sprint 的工作成果，是「從工作目錄帶進來」的 untracked 檔。

**內容評估：**

這是一個 OpenAI gpt-4o-mini-tts PoC（TTS 比較用），功能是「說」這層的備用研究，不是 MuseTalk「嘴」這層的一部分。

**歸屬判斷：**

| 判斷維度 | 結論 |
|---|---|
| 是否屬於 v0.7 scope？ | 否，v0.7 scope = 嘴（MuseTalk）+ 介面。TTS 屬於「說」這層，v0.7 主計畫書未列入 |
| 是否在 docs/ 中有說明？ | 否，沒有對應的 docs 文件 |
| 對 v0.7 deploy 是否有風險？ | 無直接風險，Modal App name = `openai-tts-poc`，不與 musetalk 衝突 |
| 是否值得保留在此 branch？ | 可保留作為 voice-path scope 研究素材，但應補 note |

**建議：保留（不 revert），但加標記**

理由：
1. 這是 calcifer 2026-05-22 的研究成果，具備長期 sprint 參考價值（「說」層 TTS 比較）
2. Revert 可能帶來不必要複雜度（stash 或 cherry-pick 都要追蹤）
3. 檔案不影響 v0.7 deploy、不影響 MuseTalk 運作

**Action Item：** 下個 commit 在 `breeze_poc/app_openai_tts.py` 頂部加一行：

```python
# NOTE: this is a Voice Path v2.0 research artifact (2026-05-22 sprint)
# Not part of v0.7 MuseTalk scope · kept for TTS comparison reference
```

同時在 ARCHITECTURE_FIX_PLAN.md 或 STATUS.md 記錄「app_openai_tts.py 存在 breeze_poc/ 下，屬 v2.0 TTS 研究用，不屬 v0.7 架構」。

**Verdict：WARN（不 block，補 note 後 OK）**

---

## § 2 · 退版路徑驗證

**目標版本（上一個能跑的 commit）：**

| 情境 | commit | branch | 說明 |
|---|---|---|---|
| v0.7 sprint 失敗 → 退 | `5fb61cf` | `voice-path/v0.3.2-tavus-cvi-poc` | v0.5.0，Tavus end-to-end 架構，中文能跑但架構錯 |
| 更早退 → main 基底 | `b590661` | `castle/main` | v0.2.0，OpenAI WebRTC 直連，無 MuseTalk |

**退版指令（已驗）：**

```bash
# 退回 v0.5.0（v0.7 失敗時的緊急退版）
cd "C:\Users\Administrator\Claude\castle-voice-engine"
git checkout voice-path/v0.3.2-tavus-cvi-poc
# 重新 deploy（Modal app = castle-voice-engine-tavus 舊版）

# 退回 v0.2.0 main（極端情況）
git checkout castle/main
# deploy castle/main 版本
```

**stash 確認：**

`stash@{0}: On voice-path/v0.3.2-tavus-cvi-poc: v0.5.1-unship-pending-arch-fix` 存在，可用。

取回 stash 指令：

```bash
git checkout voice-path/v0.3.2-tavus-cvi-poc
git stash pop stash@{0}
```

**退版路徑完整、指令已驗。**

---

## § 3 · 下次 commit 必過的 visual gate 提醒（給卡西法接續 implement）

當下一批 commit 包含以下任一檔案時，必觸發 4 道 visual gate，不得跳過：

**觸發清單：**

- `castle/static/index.html`
- `castle/static/voice-lifecycle.js`
- `castle/static/manifest.json`
- `castle/static/sw.js`
- `castle/static/*.css`
- `castle/static/*.png`（portrait / icon）
- 任何 `castle/static/` 下的檔案

**4 道 visual gate 必跑清單（每批 commit 獨立跑）：**

| Gate | 動作 | 工具 | 完成標準 |
|---|---|---|---|
| G1 看到了沒 | chrome MCP navigate 到 deploy URL + screenshot（save_to_disk:true） | Chrome MCP | 截圖存在且可讀，非空白、非 error |
| G2 走完互動了沒 | 依女巫 spec § 5 G2 的 7 個互動路徑逐一 click + screenshot | Chrome MCP | 每路徑有 before/after 截圖，狀態切換可見 |
| G3 COO 三問 | 對著截圖回答：老闆說 ship 得出去？老闆會點頭？老闆說這代表我們？ | 目視 + 截圖 | 三問全過才報 done，未過自己改 |
| G4 對齊 Duix reference | 比對 ARCHITECTURE_FIX_PLAN.md § E 列表，逐項核對 | 目視 + 截圖 | § E 全 ✅，不相符的改到一致 |

**5 個視窗尺寸都跑（女巫 spec § 2 要求）：**

1. 桌面全屏 1920×1080
2. 桌面中等 800×600
3. 平板直立 768×1024
4. iPhone 16 393×852
5. 桌面浮窗 PIP（PIP 進入後截圖）

**6 個狀態都跑（女巫 spec § 1.3 要求）：**

hibernate / waking / pre-call / in-call / soft-prompt / semi-idle

**最少截圖數：** 5 尺寸 × 6 狀態 = 30 張（基本），加 7 個互動路徑 = 最少 44 張。

**沒截圖 = 不算 PASS，不能 ship 給 Edward。**

---

## § 4 · 整體 Verdict

### 六件 Check 匯總

| Check | 結果 | 備注 |
|---|---|---|
| Check 1 · 分支策略 | **PASS** | main 乾淨，branch 從 v0.3.2 分出，stash 存在，退版指令已驗 |
| Check 2 · 第 5 道 gate enforcement | **PASS** | dependency 逐一對齊官方 tested matrix，既有 server/breeze 0 改動 |
| Check 3 · Sally hard rule 落地 | **PASS** | enroll + stream_url 都過 `_check_subject`，無 bypass，服務端 gap 列 WARN backlog |
| Check 4 · Attribution 落地 | **PASS** | README + 兩個 Python file header + log header 全落地（Track A 必要項） |
| Check 5 · 4 道 visual gate | **PASS** | 本 commit 無視覺改動，skip 正確 |
| Check 6 · app_openai_tts.py | **WARN** | 非 v0.7 scope，可保留，需補 note |

### 整體 Verdict

**CONDITIONAL（補一件才 unblock）**

**條件（本 sprint 繼續前必完成）：**

1. **WARN-1**（Check 3）：`app_musetalk.py` 服務端 `/musetalk/enroll` endpoint 加服務端 subject_guard 呼叫（castle.safety 無法進 Modal image 的問題，可用 inline 規則或環境變數黑名單替代，不必整個 module import）。方案：在 enroll endpoint 加：
   ```python
   DENIED = {"sally", "minor", "child", "stranger", "visitor"}
   if subject.lower() in DENIED:
       raise HTTPException(status_code=403, detail="subject denied by Sally hard rule")
   ```
   這樣即使 client 端漏過，服務端也有防線。

2. **WARN-2**（Check 6）：`breeze_poc/app_openai_tts.py` 頂部加 2 行 note，說明這是 v2.0 TTS 研究素材，非 v0.7 scope。

**兩件 WARN 補完後 = 可繼續推進 Phase 2（卡西法 implement 視覺層）。**

**沙利曼 P0 checklist 進度（本 commit 覆蓋）：**

| P0 項目 | 本 commit 狀態 |
|---|---|
| C1 MuseTalk MIT license verify | ✅ commit message + file header 確認 |
| C12 subject_guard 複用 | ✅ client 端完整落地，服務端需補（WARN-1）|
| C4 attribution 四項 | ✅ README + Python header + log header 全過 |
| C8 BREEZE_AUTH_TOKEN grep | ⏳ 本 commit 未觸及 BREEZE_AUTH_TOKEN，但 Edward 需自行跑 `git log --all -p \| grep BREEZE_AUTH_TOKEN` 確認 0 命中（Gate 4 不代替執行，需 Edward 親確認）|
| C5 Modal A10 SKU verify | ⏳ 需 Edward 親跑 Modal dashboard 確認（Gate 4 無法代替）|

---

## § 5 · Action Items 清單

| # | 項目 | 責任人 | 優先 | 驗收 |
|---|---|---|---|---|
| A1 | `app_musetalk.py` 服務端 /musetalk/enroll 加 inline DENIED set（Sally hard rule 服務端補防） | 卡西法 | P0 · 下次 commit 前 | grep `DENIED` 在 enroll function 內 |
| A2 | `breeze_poc/app_openai_tts.py` 頂部加 NOTE 說明非 v0.7 scope | 蘇菲（任何人）| P1 · 下次 commit 帶入 | 檔案頭 2 行有 NOTE |
| A3 | `git log --all -p \| grep BREEZE_AUTH_TOKEN` 結果 Edward 親跑確認 0 命中 | Edward | P0 · Modal deploy 前 | Edward confirm 0 hit |
| A4 | Modal dashboard budget alert / spending cap verify | Edward | P0 · Modal deploy 前 | Edward confirm 有 alert，或補 cron |
| A5 | 下批視覺 commit（castle/static/ 任何檔）前，卡西法跑 30+ 張 chrome MCP screenshot | 卡西法 | 下批 ship 前強制 | 截圖 ≥ 30 張，女巫複審通過 |
| A6 | Gate 4 下批 commit 完整跑馬魯克巡檢（含視覺 gate 證據）| 馬魯克 | 下批 ship 前 | 本巡檢報告更新版 v2 |

---

*Gate 4 巡檢報告 v1.0 · 2026-05-23 · 馬魯克 · 整體 CONDITIONAL（A1 + A2 補完即可解鎖 Phase 2）*
