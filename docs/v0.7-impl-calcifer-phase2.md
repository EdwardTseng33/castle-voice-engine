# v0.7 純黑 Duix 介面 implement 結果 · 卡西法 Phase 2 後半段

> 2026-05-23 · castle-voice-engine voice-path/v0.7-musetalk-self-host branch
> 主對話蘇菲派工 · 落地女巫計畫書 § 1-3 + 自己 plan § 3.4 + § 3.5
> 4 道 visual gate 自審完 · 12 / 12 PASS

---

## § 1 · 6 件落地狀態

| 件 | 路徑 | 狀態 | 行數 / 大小 |
|---|---|---|---|
| 1. castle/static/index.html v0.7 純黑 Duix | castle-voice-engine/castle/static/index.html | **PASS** | 653 行 / 23 KB |
| 2. castle/static/voice-lifecycle.js | castle-voice-engine/castle/static/voice-lifecycle.js | **PASS** | 164 行 / 5.2 KB |
| 3. castle/static/manifest.json | castle-voice-engine/castle/static/manifest.json | **PASS** | 23 行 / 634 B |
| 4. castle/static/sw.js | castle-voice-engine/castle/static/sw.js | **PASS** | 80 行 / 2.3 KB |
| 5. castle/static/sophie-portrait.png | castle-voice-engine/castle/static/sophie-portrait.png | **PASS** | 1.9 MB · 從 Moving Castle/public/assets 拷貝 |
| 6. PWA icon 4 size | （未做） | **WARN · 留下個 sprint** | manifest 暫用 sophie-portrait.png 當 icon fallback |

每件實作對齊：

- **index.html v0.7**：完整實作女巫 § 1.1 HTML 結構 + § 1.2 CSS（純黑 + 暖琥珀全砍）+ § 1.3 6 階段 state machine 觸發 + § 2.1 響應式 8 breakpoint（mobile-first base + tablet + desktop med + desktop full + iPhone 16 base + iPhone 16 Plus + Dynamic Island safe-area + landscape）+ § 3 PWA register + § 1.4 a11y focus-visible + reduced-motion。整合 OpenAI realtime /sdp POST WebRTC（既有 backend）+ MuseTalk WS /musetalk/stream（待 Modal 上線）+ session.update turn_detection 配置（女巫 + 我自己 plan § 3.5）+ mic/cam toggle + PIP + Esc 結束 + beforeunload cleanup + dev helper window.__sophie 跨階段切換 hook。

- **voice-lifecycle.js**：完整 6-state machine class · STATES enum + TIMERS 配置（30s soft-prompt / 60s semi-idle / 5 min hibernate）+ transitionLocked 400ms 鎖避免 race condition + markVoice / markFace external signal + wake / startCall / endCall 入口 + _notifySleep POST /voice/sleep best-effort + tick() 1s 自動推進。對齊我自己 plan § 3.4 完整骨架 + 過渡 contract 5 條。

- **manifest.json**：對齊女巫 § 3.1 · standalone + window-controls-overlay + theme_color/background_color 純黑 + start_url + scope + shortcuts「立刻喚醒」。icon array 暫只 1 個（512×512 用 sophie-portrait.png 當 fallback）· 4 size icon 留下個 sprint。

- **sw.js**：對齊女巫 § 3.3 完整 source · CACHE_VERSION + SHELL_ASSETS（index/lifecycle/portrait/manifest）+ NEVER_CACHE（**含 /musetalk/ /sdp /voice/ /breeze/ /realtime/ /health /session/**）+ install precache + activate cache cleanup + fetch stale-while-revalidate 策略。ES5 syntax compat（var + function expression · 不用 arrow）。

- **sophie-portrait.png**：1.9 MB · 1024×1024+ 解析度 · 直接從 Moving Castle/public/assets/sophie-portrait-original.png 原檔拷貝 · 不重新壓縮 / 不失真。


---

## § 2 · 4 道 visual gate 自審結果

### G1 看到了沒（chrome MCP screenshot · 12 張）

跑 4 階段 × 3 尺寸 = **12 張 screenshot 全部 PASS**。

| 階段 | Desktop 1280×720 | Tablet 768×1024 | Mobile 393×852 (iPhone 16) |
|---|---|---|---|
| hibernate | PASS · 純黑 + portrait cover + 蘇菲名 + hint + watermark | PASS · 同左 + breakpoint 字體調 | PASS · safe-area-inset 對 + 字體調小 |
| waking | PASS · portrait + spinner（中央偏下）+「準備中」label | PASS · 同左 | PASS · 同左 |
| pre-call | PASS · portrait fallback（stream-ready=0）+ 綠 Start 鈕底部正中 | PASS · 同左 | PASS · 同左 + Start 鈕 padding 12px |
| semi-idle | PASS · 跟 hibernate 一致 + hint 顯示 | PASS · 同左 | PASS · 同左 |

每張對齊女巫 § 1.4 預期視覺描繪：

- 純黑底 ✓
- 蘇菲 italic 名 ✓
- info dot ✓
- hint「叫我或對我看一眼就會醒」呼吸感 ✓
- waking spinner + 「準備中」label ✓
- 綠 Start 鈕 pill 圓 + 📞 icon + 綠 glow shadow ✓
- 「Powered by 城堡」極淡 watermark ✓
- **無任何暖琥珀** ✓
- Georgia italic + 中文 serif fallback ✓

**不能拍的 2 階段（in-call + soft-prompt）**：需真實 Modal MuseTalk video stream 進來才有 video frame · 等 Edward 拍板 Modal cost 後跑。

### G2 互動了沒（state machine + DOM 屬性測試）

| 互動 | 動作 | 結果 |
|---|---|---|
| state machine 6 階段切換 | window.__sophie.setState() 連跑 6 states，每階段 500ms wait 過 transitionLocked | **6 / 6 PASS** |
| manifest fetch | fetch /static/manifest.json + parse + name 比對 | PASS |
| sw fetch | fetch /static/sw.js + 比對 NEVER_CACHE 存在 | PASS |
| Service Worker 註冊 | navigator.serviceWorker.getRegistration() 拿 active 物件 | PASS |
| VoiceLifecycle class load | typeof window.VoiceLifecycle === function | PASS |
| portrait image load | getElementById portrait naturalWidth > 0 | PASS |
| Console error | preview_console_logs level=error | **0 error** |
| Console warn | preview_console_logs level=warn | **0 warn** |

7 個互動路徑中、本機可直接驗的 6 個都 PASS。剩下 1 個（PIP / mic toggle / cam toggle 涉 getUserMedia 真實麥克風 + camera）= 本機 localhost http 無 SSL → getUserMedia secure context 限制、必須 https 才能跑 · 留 Modal deploy 後跑（Modal 自動 https）。


### G3 COO 三問

1. **「老闆會說 ship 得出去嗎？」**
   - 純黑底 + Georgia italic + safe-area-inset + reduced-motion + a11y focus-visible 全到位、視覺對齊 Edward 5/23 拍板「乾淨俐落」標準
   - 但 in-call / soft-prompt 2 階段需真實 Modal video 才能拍 = **本批 ship spec ready · 真 ship 給 Edward 看 in-call 體驗必過 Modal 上線**
   - PASS（spec ship 層）· in-call 體驗層待 Modal

2. **「老闆會點頭嗎？」**
   - 6 階段 state machine 跑通 + 響應式 5 尺寸 + PWA manifest + Service Worker + OpenAI realtime + MuseTalk WS 接通骨架 + turn_detection 配置全部到位
   - 對齊 ARCHITECTURE_FIX_PLAN § E（純黑視覺）+ § G（PWA + PIP + 響應式）+ Edward 三層分工拍板
   - PASS

3. **「老闆會說這代表我們嗎？」**
   - 純黑 Duix 範式 + 寫實短髮藍領 Sophie reference + Georgia italic + 對外場景（會議 / 訪談 / 代理人）符合 Voice Path 對外分身願景
   - 沒漂亮的 Q 版 + 沒花俏動畫 + 沒暖琥珀 = 對外場景拿得出手
   - PASS

### G4 對齊 Duix reference

對齊 ARCHITECTURE_FIX_PLAN § E（Edward 親自看 Duix 後寫的 source of truth）+ 女巫 § 1.4 預期視覺：

- ✓ 整體背景純黑 #000
- ✓ 視訊區大放中央（live-video object-fit cover）
- ✓ top-left「蘇菲 ⓘ」italic 白字 + 半透明 line border info dot
- ✓ top-right 空（settings 鈕砍掉 · v0.5.0 那顆已移除）
- ✓ bottom-left timer tabular-nums monospace
- ✓ bottom-center single 綠 Start 鈕（pre-call）/ 3 圓鈕 mic/cam/end + PIP（in-call）
- ✓ bottom-right「Powered by 城堡」極淡 11px 字
- ✓ 整體無暖琥珀 / 無漸層 / 無花俏 / 無 emoji decoration（只有 functional 📞 / 🎙 / 📷 / ⛶ / ●）

**PASS**。

---

## § 3 · 已知限制

### 3.1 視覺自審層

1. **in-call + soft-prompt 2 階段無真實 video** · 必待 Modal MuseTalk app 上線、portrait fade out → live-video fade in cross-fade 才能驗
2. **7 互動路徑全跑只能等 https 環境** · 本機 localhost http 沒 getUserMedia secure context · Modal deploy 後跑
3. **Duix.ai 真實 reference 對比** · Duix.ai 主站「隱私權設定發生錯誤」開不了 · 等 Edward 重給可達連結 / WebFetch 抓公開 demo screenshot

### 3.2 PWA 完整層

1. **PWA icon 4 size 未做** · manifest 暫用 sophie-portrait.png 當 512×512 fallback · 真正 PWA 安裝體驗需 192/512/maskable-512/monochrome-512 四個獨立 icon · 留下個 sprint（PIL 或 canvas 30 分鐘生）

### 3.3 Backend 整合層

1. **MuseTalk Modal app 未 deploy** · WS endpoint /musetalk/stream 在 browser 端 connect 會 fail · 已加 graceful error handling（WS close → stream-ready=0 → portrait fallback 繼續顯示）
2. **OpenAI realtime /sdp 需真實 OPENAI_API_KEY** · 本機 dev server 沒掛 backend route · 必須跑完整 FastAPI app（app.py）才能測 SDP exchange
3. **Breeze ASR Modal app 整合層** · 介面層沒直接連 Breeze（女巫 spec + 我自己 plan § 2.3 把 Breeze 當 backend-only 用、不暴露 WS）· 介面只連 /sdp + /musetalk/stream 兩條 stream

### 3.4 不衝守則執行狀況

- ✓ 不衝 in-call / soft-prompt 階段（沒真實 video stream · 沒跑 fail）
- ✓ 不動 castle/server/realtime_endpoints.py
- ✓ 不動 breeze_poc/app_breeze.py
- ✓ 不動既有 castle/integrations/（subject_guard / spacy_ner / picovoice / speechbrain_voiceid / tavus_client / musetalk_client 全部 untouched）
- ✓ index.html 內整合對接走 /sdp POST + /musetalk/stream WS · 不衝既有 endpoint
- ✓ Tavus daily-js 整套對接捨棄（v0.5.0 → v0.7 介面從頭寫·OpenAI realtime 主路）


---

## § 4 · Verdict

### **READY for Modal deploy + Edward 操作體驗** ✅

**理由**：
1. 6 件落地狀態 5 PASS + 1 WARN（PWA icon · 不阻 ship）
2. G1 12 / 12 screenshot PASS（4 階段 × 3 尺寸 · 純黑 Duix DNA 全到位）
3. G2 6 / 7 互動 PASS（剩 1 個 getUserMedia 需 https · Modal deploy 自然解）
4. G3 COO 三問 3 / 3 PASS
5. G4 對齊 ARCHITECTURE § E + 女巫 § 1.4 全 8 點 PASS
6. Console 0 error / 0 warn
7. State machine 6 階段 transitions 全 PASS
8. SW + manifest + portrait + lifecycle 全 fetch 通

### 下一步（給主對話蘇菲 / Edward）

**等 Edward 拍板 Modal cost 後**：
1. deploy breeze_poc/app_musetalk.py（我自己 plan § 3.1 · 約 250 行骨架已寫）
2. 跑 MuseTalk lipsync PoC 拍嘴對齊品質 sample（A10G $1.10/hr × 5 min cold start ≈ $0.1）
3. 接通 /musetalk/stream WS · 真實 video frame 進來 → stream-ready=1 → portrait fade out → live-video fade in
4. 拍完整 12 + 2 = 14 階段 × 3 尺寸 = 42 張 screenshot（含 in-call + soft-prompt）
5. 跑 7 個互動路徑（hibernate → waking → pre-call → in-call → soft-prompt → semi-idle → hibernate 完整路徑 + PIP + end 鈕 + mic/cam toggle）

**本批 ship 範圍**：v0.7 介面 + state machine + PWA + OpenAI realtime 對接骨架 + MuseTalk WS 對接骨架 + 12 張 screenshot G1/G2 自審 PASS。可 commit + push、不衝 Modal cost。

---

## § 5 · Screenshot 路徑

本批 12 張 screenshot 透過 chrome MCP preview_screenshot 拍、image 直接 inline 在卡西法 thread output（base64 jpeg）· 不落實體 png 檔（preview_screenshot 工具不支援 save_to_disk）。

未來補拍 in-call + soft-prompt 時、screenshot 落地 castle-voice-engine/docs/v0.7-screenshots/ 目錄。

---

*v1.0 · 2026-05-23 · 卡西法 · castle-voice-engine voice-path/v0.7-musetalk-self-host branch · 接續 0f7925a Gate 4 補強*
